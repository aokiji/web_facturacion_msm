:javascript
  //localize dates
  $(document).ready(function (){
    $('.date').each( function(){
      date = $.datepicker.parseDate('yy-mm-dd', $(this).text().trim());
      $(this).text($.datepicker.formatDate($.datepicker.regional['es'].dateFormat, date));
    });
   });
#presupuesto  
  %h1 Listado de Presupuestos
  .todos
    %table
      %tr
        %th Numero
        %th Fecha
        %th Cliente
        %th{:class => 'todos_menu'}
        %th{:class => 'todos_menu'}
        %th{:class => 'todos_menu'}

      - @presupuestos.each do |presupuesto|
        %tr
          %td
            = presupuesto.number
          %td.date
            = presupuesto.created_at
          %td
            = presupuesto.cliente
          %td{:class => 'todos_menu'} 
            = link_to 'Ver', presupuesto
          %td{:class => 'todos_menu'} 
            = link_to 'Editar', edit_presupuesto_path(presupuesto)
          %td{:class => 'todos_menu'} 
            = link_to 'Eliminar', presupuesto, :confirm => '¿Estas seguro?', :method => :delete
  .piepagina
    = link_to 'Nuevo presupuesto', new_presupuesto_path