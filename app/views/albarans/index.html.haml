- content_for :javascript do
  = javascript_include_tag 'jquery.ui.min'
  = javascript_include_tag 'jquery.datepicker.es'
:javascript
  //localize dates
  $(document).ready(function (){
    $('.date').each( function(){
      date = $.datepicker.parseDate('yy-mm-dd', $(this).text().trim());
      $(this).text($.datepicker.formatDate($.datepicker.regional['es'].dateFormat, date));
    });
   });
  
%h1 Listado de Albaranes

%table
  %tr
    %th Numero
    %th Fecha
    %th Cliente
    %th
    %th
    %th

  - @albarans.each do |albaran|
    %tr
      %td
        = albaran.number
      %td.date
        = albaran.created_at
      %td
        = albaran.cliente
      %td 
        = link_to 'Ver', albaran
      %td 
        = link_to 'Editar', edit_albaran_path(albaran)
      %td 
        = link_to 'Eliminar', albaran, :confirm => '¿Estas seguro?', :method => :delete

%br

= link_to 'Nuevo Albaran', new_albaran_path
